# ===== tcpdump capture settings (edit as needed) =====
IFACE=enp0s3              # you can update "enp0s8" or "any" as per your conf
PCAP_DIR=/var/lib/tcpdump # where rotated pcaps go
ROTATE_SEC=300            # rotate every 5 minutes
KEEP_FILES=288            # keep ~24h @ 5-min rotations (288 files)
BPF_FILTER='port 53'
TCPDUMP_USER=tcpdump      # drop privileges to this user after open
